{% extends 'base.html.twig' %}

{% block custom_title %}Ajouter une tâche
{% endblock %}

{% block body %}
	<div id="content">

		{{ form_start(form) }}

		<div>{{ form_row(form.title) }}<div>

        <div>{{ form_row(form.description) }}</div>

        <div>{{ form_row(form.date) }}</div>

        <div class="flex-form">
			<div>{{ form_row(form.status) }}</div>
		</div>

		<div>{{ form_row(form.members) }}</div>

		<button class="button button-submit">Ajouter</button>

		{{ form_end(form)}}
        
    </div>
{% endblock %}