{% extends 'base.html.twig' %}

{% block custom_title %}Modifier une tâche
{% endblock %}

{% block body %}
	<div id="content">

		{{ form_start(form) }}

		<div>{{ form_row(form.title) }}<div>

        <div>{{ form_row(form.description) }}</div>

        <div class="flex-form">
			<div>{{ form_row(form.status) }}</div>
		</div>

		<div>{{ form_row(form.members) }}</div>

		<button class="button button-submit">Modifier</button>

		<a href="{{ path('app_tasks_delete', { id: id }) }}" class="delete-task">Supprimer la tâche</a>

		{{ form_end(form)}}
        
    </div>
{% endblock %}
