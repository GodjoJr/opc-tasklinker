{% extends 'base.html.twig' %}

{% block custom_title %}<h1>
	Ajouter un projet
</h1>
{% endblock %}

{% block body %}

	<div id="content">

		{{ form_start(form) }}

		<div>{{ form_row(form.title) }}</div>

		<div>{{ form_row(form.members) }}</div>

		<button class="button button-submit">Continuer</button>

		{{ form_end(form) }}
	</div>
{% endblock %}
